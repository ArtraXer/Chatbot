"""Punto de entrada compatible para importaciones desde la raíz del proyecto.

Mantiene estable la API histórica del repositorio y reexporta el runtime del agente.
"""

from core.agent_runtime import *
